# V3.2 SRE 智能运维平台 — 功能操作手册

> 版本: 3.2.0 | 更新日期: 2026-05-31 | 适用对象: SRE 运维工程师 / 平台管理员

---

## 目录

1. [平台概述](#1-平台概述)
2. [快速上手](#2-快速上手)
3. [仪表盘操作](#3-仪表盘操作)
4. [资产管理](#4-资产管理)
5. [告警管理](#5-告警管理)
6. [AI 智能助手](#6-ai-智能助手)
7. [操作工单管理](#7-操作工单管理)
8. [原子操作中心](#8-原子操作中心)
9. [API 接口说明](#9-api-接口说明)
10. [常见操作场景](#10-常见操作场景)

---

## 1. 平台概述

### 1.1 平台定位

SRE 智能运维平台（Engineer Assist）是一个面向数据中心运维场景的智能辅助系统。核心理念是"让数据中心每个人都能做一线运维"——通过 AI 对话界面降低运维操作门槛，同时保留完整的审批流和安全控制。

### 1.2 核心能力

| 能力 | 说明 | 入口 |
|------|------|------|
| 智能对话 | 自然语言输入，自动识别意图并推荐操作步骤 | 聊天界面 |
| 资产管理 | 统一管理所有基础设施资产（VM/物理机/存储/网络） | 资源管理 |
| 告警管理 | 集中查看和处理监控告警 | 告警中心 |
| 操作工单 | 创建 → 审批 → 执行 → 回滚的完整操作工作流 | 操作中心 |
| 数据仪表盘 | 全局资源、告警、操作的可视化概览 | 仪表盘 |
| 原子操作 | 109 个预定义的标准化运维操作 | 操作中心 |

### 1.3 用户角色

| 角色 | 权限 | 典型用户 |
|------|------|---------|
| viewer（查看者） | 查看仪表盘、资产、告警 | 值班人员 |
| operator（操作员） | 创建操作、执行低风险操作 | 一线运维 |
| admin（管理员） | 审批操作、管理系统配置 | SRE 负责人 |
| auditor（审计员） | 查看审计日志 | 合规人员 |
| ai_agent（AI 代理） | API 访问 | 自动化系统 |

---

## 2. 快速上手

### 2.1 访问平台

部署完成后，通过浏览器访问：

```
开发环境: http://localhost:5173
生产环境: https://sre.example.com
```

### 2.2 登录

首次访问无需认证（开发模式默认使用 dev-user 身份），生产环境通过 SSO 或 JWT 令牌认证。

### 2.3 界面导航

```
+--------------------------------------------------------------+
| 侧边栏 (左侧)             |  主内容区域                        |
|                           |                                   |
|  ☰ SRE 平台              |  [仪表盘] [聊天] [资源] ...        |
|                           |                                   |
|  📊 仪表盘               |  根据当前页面显示对应内容           |
|  💬 智能助手              |                                   |
|  🖥️ 资源管理             |                                   |
|  🔔 告警中心             |                                   |
|  ⚙️ 操作中心             |                                   |
|                           |                                   |
|  👤 当前用户              |                                   |
+--------------------------------------------------------------+
|  状态栏: API 连接状态 | 延迟 | 版本号                        |
+--------------------------------------------------------------+
```

### 2.4 键盘快捷键

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+K` | 打开命令面板 |
| `Esc` | 关闭弹窗/面板 |
| `/` | 聚焦聊天输入框 |
| `Enter` | 发送消息 |
| `Shift+Enter` | 换行 |

---

## 3. 仪表盘操作

### 3.1 访问路径

侧边栏 → **仪表盘** 或 URL: `/dashboard`

### 3.2 功能说明

仪表盘提供全局运维状态的快速概览，包含以下模块：

#### 3.2.1 统计卡片
在页面顶部展示关键数字：
- **总资产数** — 平台管理的所有基础设施资产总数
- **活跃告警** — 当前 firing 状态的告警数量
- **待审批操作** — 等待审批的操作工单数
- **资产按状态分布** — 运行中/维护中/异常等

#### 3.2.2 资源使用图表
展示集群级别的资源使用情况柱状图，包括：
- 各集群 CPU 使用率
- 各集群内存使用率
- 各集群存储使用率

#### 3.2.3 告警分布
饼图展示按严重级别划分的告警分布：
- P0（严重）— 红色
- P1（高）— 橙色
- P2（中）— 黄色
- P3/P4（低/信息）— 蓝色

#### 3.2.4 最近操作
列出最近 5 条操作工单，包含操作标题、状态、创建时间。

#### 3.2.5 趋势图表
7 天趋势折线图，展示 CPU 使用率、内存使用率和告警数量的变化趋势。

### 3.3 操作步骤

```bash
# 1. 进入仪表盘
点击侧边栏「仪表盘」

# 2. 刷新数据
点击右上角「刷新」按钮

# 3. 查看详情
点击趋势图表上的数据点可查看具体数值

# 4. 查看告警详情
点击告警分布图的扇区可跳转到告警列表
```

---

## 4. 资产管理

### 4.1 访问路径

侧边栏 → **资源管理** 或 URL: `/resources`

### 4.2 功能说明

资产管理模块提供对所有基础设施资产的统一管理，支持以下操作：

#### 4.2.1 查看资产列表

**页面布局**：
```
+--------------------------------------------------------------+
| 🔍 [搜索框]  [类型筛选▾] [平台筛选▾] [状态筛选▾]           |
+--------------------------------------------------------------+
| 资产列表 (分页显示)                                           |
| +----------------------------------------------------------+ |
| | 名称 | 类型 | IP地址 | 平台 | 状态 | 创建时间 | 操作 |    | |
| |------|------|--------|------|------|---------|------|    | |
| | web-01 | VM | 10.0.0.1 | vSphere | 运行中 | ... | 详情 |  | |
| | db-01  | 数据库 | ... | Physical | 维护 | ... | 详情 |  | |
| +----------------------------------------------------------+ |
| [上一页] 第 1/10 页，共 500 条 [下一页]                    |
+--------------------------------------------------------------+
```

**筛选条件**：
- 按类型筛选：VM / 物理机 / 存储 / 网络 / 数据库 / 容器
- 按平台筛选：vSphere / OpenStack / Kubernetes / Physical
- 按状态筛选：运行中 / 已停止 / 错误 / 维护中 / 未知
- 模糊搜索：按名称或主机名搜索

#### 4.2.2 创建资产

```bash
# API 方式
POST /api/v1/assets
Content-Type: application/json

{
  "name": "web-01",
  "asset_type": "vm",
  "platform": "vSphere",
  "ip_address": "10.0.0.1",
  "hostname": "web-01.example.com",
  "environment": "production",
  "tags": {"role": "web", "team": "platform"}
}
```

#### 4.2.3 执行快速操作

在资产详情页，可以对资产执行快速操作：

| 操作 | 需要确认 | 风险等级 | 说明 |
|------|---------|---------|------|
| 开机 (boot) | 否 | 低 | 启动已关机的虚拟机 |
| 关机 (shutdown) | 是 | 高 | 优雅关闭虚拟机 |
| 重启 (restart) | 是 | 中 | 重启虚拟机 |

**操作示例**：
```bash
POST /api/v1/assets/42/actions
{
  "action": "restart",
  "confirm": true
}

# 响应
{
  "success": true,
  "action": "restart",
  "message": "Action 'restart' executed on asset 'web-01'",
  "operation_id": null
}
```

---

## 5. 告警管理

### 5.1 访问路径

侧边栏 → **告警中心** 或 URL: `/alerts`

### 5.2 告警级别

| 级别 | 颜色 | 含义 | SLA |
|------|------|------|-----|
| P0 | 🔴 红色 | 服务不可用 | 15 分钟响应 |
| P1 | 🟠 橙色 | 功能严重受损 | 30 分钟响应 |
| P2 | 🟡 黄色 | 部分影响 | 2 小时响应 |
| P3 | 🔵 蓝色 | 轻微影响 | 8 小时响应 |
| P4 | ⚪ 灰色 | 信息通知 | 24 小时响应 |

### 5.3 告警生命周期

```
firing → acknowledged → resolved
  ↓                        ↓
silenced                expired
```

### 5.4 操作步骤

#### 5.4.1 查看告警列表

```bash
# 获取所有活跃告警
GET /api/v1/alerts?status=active&severity=P0,P1

# 响应
{
  "items": [
    {
      "id": 1,
      "title": "web-01 CPU 使用率超过 90%",
      "severity": "P1",
      "status": "active",
      "source": "prometheus",
      "created_at": "2026-05-31T10:00:00Z"
    }
  ],
  "total": 1,
  "page": 1,
  "limit": 20
}
```

#### 5.4.2 确认告警

当确认收到告警并开始处理时：

```bash
PUT /api/v1/alerts/1/acknowledge
{
  "notes": "已确认，开始排查 CPU 高负载原因"
}
```

#### 5.4.3 解决告警

当告警原因已消除时：

```bash
PUT /api/v1/alerts/1/resolve
{
  "resolution_notes": "发现 web-01 上运行了异常的日志处理进程，已终止。CPU 使用率恢复正常。"
}
```

#### 5.4.4 告警处理最佳实践

1. 🔴 P0 告警: 立即在聊天中发送"查看告警详情" → 确认 → 创建操作工单进行处理
2. 🟠 P1 告警: 确认告警 → 评估影响范围 → 安排处理
3. 🟡 P2 告警: 记录到操作工单 → 在维护窗口处理
4. 🔵 P3/P4: 定期审查，确认后关闭

---

## 6. AI 智能助手

### 6.1 访问路径

侧边栏 → **智能助手** 或 URL: `/chat`

### 6.2 支持的对话场景

智能助手支持通过自然语言进行以下操作：

#### 6.2.1 故障诊断

| 输入示例 | 识别意图 | 推荐操作 |
|---------|---------|---------|
| "web-01 连接超时" | diagnose | ping + VM 状态检查 |
| "db-01 连接不上" | diagnose | ping + VM 状态检查 |
| "web-01 宕机了" | diagnose | ping + VM 状态检查 |
| "web-01 怎么了" | diagnose | ping + VM 状态检查 |

#### 6.2.2 操作执行

| 输入示例 | 识别意图 | 推荐操作 |
|---------|---------|---------|
| "重启 web-01" | restart | VM 重启 |
| "关机 web-01" | shutdown | VM 关机 |
| "开机 web-01" | power_on | VM 开机 |
| "给 web-01 创建快照" | snapshot | 创建 VM 快照 |
| "迁移 web-01" | migrate | VM 迁移 |

#### 6.2.3 状态查询

| 输入示例 | 识别意图 | 推荐操作 |
|---------|---------|---------|
| "查看告警" | check_alerts | 查询活跃告警 |
| "看看集群资源" | check_resources | 查询主机状态 |
| "查看 web-01 状态" | check_status | VM 状态 + 主机状态 |
| "web-01 磁盘满了" | disk_usage | 磁盘使用检查 |
| "检查 web-01 网络" | network_check | ping + traceroute |

#### 6.2.4 数据库操作

| 输入示例 | 识别意图 | 推荐操作 |
|---------|---------|---------|
| "检查 db-01 数据库状态" | db_status | 数据库状态 + 连接池 |
| "db-01 慢查询" | db_slow_queries | 慢查询分析 |
| "检查 db-01 同步延迟" | db_replication | 复制延迟检查 |
| "db-01 备份状态" | db_backup | 备份状态检查 |

#### 6.2.5 安全操作

| 输入示例 | 识别意图 | 推荐操作 |
|---------|---------|---------|
| "扫描 web-01 漏洞" | security_scan | 漏洞扫描 |
| "web-01 补丁状态" | patch_check | 补丁状态检查 |
| "检查 web-01 证书" | cert_check | 证书到期检查 |
| "续签 web-01 证书" | cert_renew | 证书续签 |

#### 6.2.6 日志和监控

| 输入示例 | 识别意图 | 推荐操作 |
|---------|---------|---------|
| "查看 web-01 日志" | log_search | 日志搜索 |
| "web-01 性能查看" | performance_check | CPU + 内存监控 |
| "查看 web-01 监控指标" | metric_query | 指标查询 |

#### 6.2.7 服务管理

| 输入示例 | 识别意图 | 推荐操作 |
|---------|---------|---------|
| "重启 web-01 上的 nginx 服务" | service_restart | 服务重启 |
| "查看 web-01 上的 nginx 服务状态" | service_status | 服务状态 |

#### 6.2.8 网络操作

| 输入示例 | 识别意图 | 推荐操作 |
|---------|---------|---------|
| "查看 web-01 防火墙规则" | firewall_list | 防火墙规则列表 |
| "检查 DNS 解析 example.com" | dns_check | DNS 解析检查 |

### 6.3 对话流程

```
用户输入 → 意图识别 → 参数提取 → 推荐步骤 → 用户确认 → 执行 → 反馈

[用户]: web-01 连接超时
[助手]: 检测到 web-01 存在问题，建议按以下步骤诊断：
        步骤1: Ping 主机 (低风险)
        步骤2: 查看 VM 状态 (低风险)
        风险评估：低风险 — 可安全执行，无需审批
        预计耗时：15 秒
[用户]: [点击"执行全部"]
[助手]: [弹窗确认] 确认执行推荐的诊断步骤？
[用户]: [确认]
[助手]: [执行中...] → [返回结果]
```

### 6.4 完整对话示例

```
===========================================================================
用户: 查看告警
助手: 以下是当前活跃的告警信息，建议按优先级处理：

      风险评估：低风险 — 可安全执行，无需审批
      预计耗时：5 秒

      推荐操作：
      ✅ 步骤1：Alert Check — 检查活跃告警

用户: web-01 连接超时
助手: 检测到 web-01 存在问题，建议按以下步骤诊断：

      风险评估：低风险 — 可安全执行，无需审批
      预计耗时：15 秒

      推荐操作：
      ✅ 步骤1：Ping Host — 检查网络可达性
      ✅ 步骤2：VM Status — 检查 VM 电源状态和健康

用户: 检查 db-01 数据库状态
助手: 检查 db-01 数据库状态和连接池：

      风险评估：低风险 — 可安全执行，无需审批
      预计耗时：15 秒

      推荐操作：
      ✅ 步骤1：DB Status — 检查数据库服务器健康
      ✅ 步骤2：Connection Pool — 检查连接池使用率

用户: 帮助
助手: 我是 Engineer Assist 智能运维助手，可以帮您：
      - 诊断故障：输入主机名+症状（如 web-01 连接超时）
      - 执行操作：重启/关机/开机 + 主机名
      - 查看告警：查看告警
      - ...
===========================================================================
```

---

## 7. 操作工单管理

### 7.1 访问路径

侧边栏 → **操作中心** 或 URL: `/operations`

### 7.2 操作工单状态流转

```
                    +---> 已取消
                    |
创建 (draft) ---> 待审批 (pending_approval) ---> 已批准 (approved) ---> 执行中 (executing) ---> 已完成 (completed)
                    |                               |                         |
                    +---> 已拒绝 (rejected)           +---> 已回滚              +---> 失败 (failed)
                                                                                    |
                                                                                    +---> 已回滚 (rolled_back)
```

### 7.3 操作步骤

#### 7.3.1 创建操作工单

```bash
POST /api/v1/operations
{
  "title": "诊断 web-01 连接问题",
  "description": "用户报告 web-01 无法通过 SSH 连接",
  "intent": "diagnose",
  "asset_id": 42,
  "params": {"target": "web-01"},
  "steps": [
    {"step_number": 1, "action": "infra.ping", "description": "Ping 检查网络可达性"},
    {"step_number": 2, "action": "compute.vm_status", "description": "检查 VM 电源状态"}
  ]
}
```

#### 7.3.2 审批工单

```bash
# 批准
PUT /api/v1/operations/1/approve
{}

# 拒绝
PUT /api/v1/operations/1/reject
{"reason": "请在维护窗口执行此操作"}
```

#### 7.3.3 查看工单状态

```bash
GET /api/v1/operations/1

# 响应包含步骤执行详情
{
  "id": 1,
  "title": "诊断 web-01 连接问题",
  "status": "executing",
  "steps": [
    {"step_number": 1, "action": "infra.ping", "status": "completed", ...},
    {"step_number": 2, "action": "compute.vm_status", "status": "running", ...}
  ]
}
```

---

## 8. 原子操作中心

### 8.1 访问路径

`GET /api/v1/atomics`

### 8.2 操作分类

原子操作按 9 大类别组织，共 109 个预定义操作：

| 类别 | 操作数 | 示例 | 风险分布 |
|------|--------|------|---------|
| infra（基础设施） | 7 | ping, traceroute, DNS 检查, 端口扫描, 带宽测试, 证书检查, WHOIS | 全部低风险 |
| compute（计算） | 11 | VM 状态, 开机, 关机, 重启, 快照, 迁移, 主机维护, CPU/内存/进程/杀进程 | 低~高 |
| storage（存储） | 5 | 磁盘使用, LUN 状态, NFS 检查, 快照列表/删除, 扩容 | 低~严重 |
| network（网络） | 6 | VLAN 检查, 防火墙规则(增/删/查), LB 状态, LB 成员(启用/禁用) | 低~严重 |
| database（数据库） | 6 | 状态, 复制延迟, 慢查询, 杀查询, 备份状态, 连接池 | 低~高 |
| security（安全） | 6 | 漏洞扫描, 补丁状态, 登录审计, 证书续签, 用户审计, 合规检查 | 低~中 |
| monitoring（监控） | 6 | 告警检查, 指标查询, 阈值设置, 静默告警, 创建仪表盘, 日志搜索 | 低~中 |
| backup（备份） | 5 | 创建/恢复/验证备份, 保留检查, 备份计划列表 | 低~严重 |
| maintenance（维护） | 7 | OS 更新, 服务重启/状态, 配置检查/应用, 日志轮转, 清理临时文件, Cron 检查, 时区检查 | 低~高 |

### 8.3 操作搜索

```bash
# 按分类筛选
GET /api/v1/atomics?category=compute

# 按关键词搜索
GET /api/v1/atomics?search=vm

# 查看操作详情
GET /api/v1/atomics/compute.vm_restart
```

---

## 9. API 接口说明

### 9.1 通用规范

- **基础路径**: `/api/v1`
- **数据格式**: JSON
- **认证方式**: Bearer Token（JWT）
- **分页参数**: `page`（从 1 开始，默认 1）, `limit`（最大 200，默认 20）
- **错误格式**:

```json
{
  "success": false,
  "error": {
    "code": "ASSET_NOT_FOUND",
    "message": "Asset with id '999' not found",
    "details": null
  }
}
```

### 9.2 接口速查

#### 健康检查

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/health` | 存活探针 |
| GET | `/ready` | 就绪探针（含 DB + Redis 检查） |
| GET | `/health/db` | 数据库健康检查 |
| GET | `/health/redis` | Redis 健康检查 |

#### 资产管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/assets` | 资产列表（支持筛选和分页） |
| GET | `/api/v1/assets/{id}` | 资产详情 |
| POST | `/api/v1/assets` | 创建资产 |
| PATCH | `/api/v1/assets/{id}` | 更新资产 |
| DELETE | `/api/v1/assets/{id}` | 删除资产 |
| POST | `/api/v1/assets/{id}/actions` | 执行快速操作 |

#### 告警管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/alerts` | 告警列表 |
| GET | `/api/v1/alerts/{id}` | 告警详情 |
| PUT | `/api/v1/alerts/{id}/acknowledge` | 确认告警 |
| PUT | `/api/v1/alerts/{id}/resolve` | 解决告警 |

#### 操作工单

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/operations` | 工单列表 |
| GET | `/api/v1/operations/{id}` | 工单详情（含步骤） |
| POST | `/api/v1/operations` | 创建工单 |
| PUT | `/api/v1/operations/{id}/approve` | 批准工单 |
| PUT | `/api/v1/operations/{id}/reject` | 拒绝工单 |

#### AI 聊天

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/v1/chat` | 发送消息并获取推荐 |

#### 仪表盘

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/dashboard/summary` | 仪表盘汇总数据 |
| GET | `/api/v1/dashboard/trends` | 趋势数据（支持 days 参数） |

#### 原子操作

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1/atomics` | 原子操作列表（支持分类/搜索） |
| GET | `/api/v1/atomics/{id}` | 原子操作详情 |

---

## 10. 常见操作场景

### 场景一：日常巡检

```
操作流程:
1. 打开仪表盘 → 查看告警数量和待审批操作
2. 如果有 P0/P1 告警 → 进入告警中心查看详情
3. 确认告警 → 创建操作工单处理
4. 查看资产管理 → 检查最近添加/变更的资产
5. 每周一次：执行完整巡检脚本
```

### 场景二：故障响应

```
用户: web-01 连接超时
助手: 检测到 web-01 存在问题，建议 ping + 状态检查
用户: 执行诊断
助手: [返回诊断结果]
      诊断结果：
      1. ping 成功（延迟 2.3ms）
      2. VM 状态：运行中
      3. 建议：检查 web 服务状态
用户: 查看 web-01 日志
助手: [返回日志搜索结果]
```

### 场景三：变更操作

```
流程:
1. 在聊天中输入 "重启 web-01"
2. AI 识别意图并推荐操作步骤
3. 如涉及高风险操作（如关机），需要确认
4. 系统创建操作工单 → 状态为"待审批"
5. 管理员审批 → 状态变为"已批准"
6. 操作员执行 → 实时查看执行进度
7. 执行完成 → 验证结果
```

### 场景四：批量操作

对于需要批量执行的操作，建议：
1. 在操作中心手动创建操作工单
2. 按步骤组织操作序列
3. 通过审批流程管控
4. 监控执行进度

### 场景五：数据查看与报表

```
操作流程:
1. 打开仪表盘 → 查看全局统计概览
2. 查看资源使用趋势图表（7 天 CPU/内存/告警趋势）
3. 使用 API 导出数据:
   GET /api/v1/assets?limit=200 → 导出所有资产列表
   GET /api/v1/dashboard/summary → 导出汇总统计
```

---

> 本文档为 V3.2 迭代版本配套操作手册。
> 如发现功能异常或有改进建议，请通过平台内的反馈渠道提交。
